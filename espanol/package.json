{
  "name": "espanol",
  "version": "0.1.0",
  "private": true,
  "description": "Spanish translation toolkit — CLI-first with optional MCP adapter",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "spanish",
    "translation",
    "i18n",
    "localization",
    "mcp",
    "cli"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "isomorphic-dompurify": "^3.6.0"
  }
}
